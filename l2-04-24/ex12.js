function calcularA(x, y) {

    return Math.sqrt(Math.pow(x, 2) + Math.powly, 3) / Math.abs(x + y);
}
function calcularB(x) {
    
    return (1 + Math.sin(x)) / (1 + Math.cos(x));
}   
function calcularC(x) {
    return 1 + (1/x)+(1/Math.pow(x, 2)) + (1 / Math.pow(x, 3)) + (1 / Math.pow(x, 4));
}
function calcularD(x, y, z) {
    
    return (x/y)(x + Math.pow((x/y), 2)) / (x - Math.pow((x/y), z));
    
    }
function calculare(x) {
    
    return Math.sqrt(Math.PI + Math.sqrt(Math.pow(Math.E, 3) + Math.sqrt(4 + Math.sqrt(x))));
    
    }